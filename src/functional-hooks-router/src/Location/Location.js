class Location {
	// constructor(router, parser, history) {
	//     const url = router.getUrl();
	//     // assign url, pathname, search, searchData, hash
	//     Object.assign(this, parser.parse(url));
	//     this.routeParams = router.getParams();
	// }
	// setPathname(pathname) {
	//     this.pathname = pathname;
	//     router.setPathname(pathname);
	// }
	// pathname,
	// setPathname,
	// routeParams,
	// setRouteParams,
	// search,
	// setSearch,
	// searchData,
	// setSearchData,
	// hash,
	// setHash,
	// url,
	// setUrl,
	// //
	// go,
	// back,
	// forward
}
